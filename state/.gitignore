logs/
# fetch_filings work area: downloads in flight, rejects, verdict logs.
staging/
joblog.tsv
# Per-run copies: one per invocation, same kind of file as joblog.tsv above.
joblog.*.tsv
*.lock.d/
facts_*.jsonl
exeval_*.txt
gaps.jsonl
